export const timing = {
  /**
   * The duration (ms) for quick animations.
   */
  quick: 300,
  /**
   * The duration (ms) for medium animations.
   */
  medium: 500,
  /**
   * The duration (ms) for long animations.
   */
  long: 1000,
};
